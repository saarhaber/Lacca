{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true
  },
  "//": "Build compiles the publishable library (src/) only. The data-collection scripts in scripts/ are dev tooling run on demand via tsx and pull optional native deps (sharp, undici) that are intentionally not bundled with the app or the GitHub Pages deploy.",
  "include": ["src/**/*.ts"]
}
